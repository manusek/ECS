# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Python

# Wirtualne środowiska (ignorujemy foldery venv/env/etc, nazwij swój folder odpowiednio)
venv/
.venv/
env/
.env/

# Skompilowane pliki Pythona (bytecode)
__pycache__/
*.pyc
*.pyo

# Pliki tymczasowe generowane przez narzędzia testowe
.pytest_cache/
.nox/
.tox/

# Pliki generowane przez aplikacje IDE (np. PyCharm, VS Code, Sublime)
.idea/
.vscode/

# Pliki konfiguracyjne specyficzne dla środowiska lokalnego (potencjalnie poufne)
# Jeśli używasz plików .env do przechowywania zmiennych środowiskowych, pamiętaj o ich zignorowaniu
.env
*.env.local

# Pliki pakietów i instalacji
*.egg-info/
dist/
build/

# Pliki dziennika (logi)
*.log

# Pliki danych specyficzne dla systemu operacyjnego (np. macOS)
.DS_Store

# Pliki generowane przez narzędzia Jupyter/IPython (jeśli dotyczy)
.ipynb_checkpoints
